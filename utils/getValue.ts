import { DataPayload } from "@/firebase";

interface RegistersProps {
  id: string;
  title: string;
  data: any[];
}
const getValue = (dataRegisters: RegistersProps[], profile: DataPayload) => {

};
export default getValue;
